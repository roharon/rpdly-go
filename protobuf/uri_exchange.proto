syntax = "proto3";

package config;
option go_package = "protobuf/uri";

service UriExchange {
  rpc GetUri(Request) returns (Response);
}

message Request {
  string origin_uri = 1;
}

message Response {
  string short_uri = 1;
}