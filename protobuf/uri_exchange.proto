syntax = "proto3";

package config;
option go_package = "protobuf/uri";

service UriExchange {
  rpc GetUri(Request) returns (Response);
  rpc SetUri(Request) returns (Response);
}

message Request {
  string uri = 1;
}

message Response {
  string uri = 1;
}